// Hello world Example
// returns a simple string

hello_world() := {
  "Hello, World!"
};

// Hello Name Example
hello_name(name: string) := {
  "Hello, "+name+"!"
};


foo() := {
    SELECT *
    FROM READ("https://raw-tutorial.s3.amazonaws.com/sales.csv")
    WHERE Seller = "Miguel"
}
